# Dashboard Environment Variables

# API URL for the FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Docusaurus URL for the textbook
NEXT_PUBLIC_DOCUSAURUS_URL=http://localhost:3000

# BetterAuth Secret (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your-secret-key-here

# Supabase Database
# 1. Create account at supabase.com
# 2. Create a new project
# 3. Go to Settings → Database → Connection string (URI)
# 4. Copy the connection string and replace [YOUR-PASSWORD] with your database password
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxx.supabase.co:5432/postgres
